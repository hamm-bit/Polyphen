# hello <name>
# this tests the IO handling of strings

# `input` is inspired by Python, defaults to string type
# however this is temporary

# in the future, when implicit type inference are implemented
# `input` will be \forall t

print ("hello" ++ input)
